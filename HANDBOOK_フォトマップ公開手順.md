# 現場フォトマップ ｜ 公開・LINE送信の手順書

2026-07-28 会社チャットのClaudeが確立。ユーザーがメーカーで作ったマップをURL化してLINEへ届けるまでの手順。

## 作成ツール

- メーカー: https://helmetto2-prog.github.io/shanaimap/maker/ （このリポジトリの maker/index.html）
- ユーザーの操作: 上空図読み込み→四角→写真→ルート→🎯自動配置→📦写真込みHTML書き出し
- 書き出しHTMLは1ファイル完結（写真base64埋め込み・印刷台帳つき・初期表示は航空写真）

## 公開手順（ユーザーが書き出しHTMLをチャットに送ってきたら）

1. 中身を確認（title・写真枚数・data:image数・aerialの有無）
2. 初期表示が図面モードだったら `<body class="showlbl">` → `<body class="showlbl photo">` に直し、
   航空写真ボタンの初期ラベルを「🗺️ 平面図」にする（古い書き出しへの後方互換）
3. このリポジトリの `genba/<ランダムな短いスラッグ>/index.html` に配置してmainへプッシュ
   - スラッグ例: hy-k3n8f（現場の頭文字+ランダム5文字。推測されにくく）
   - URL: `https://helmetto2-prog.github.io/shanaimap/genba/<スラッグ>/`
4. **LINEでユーザー本人へ自動送信**する（ユーザー指示 2026-07-28: 「相手を選ぶより自分に送りつけて」）:
   - fx-race-line リポジトリの GitHub Actions `notify.yml` を workflow_dispatch で起動
   - inputs.message に「🗺️ 題名\n\n📍 URL \n\n説明ひとこと」（改行は `\n` と書く。URLの前後に空白か改行必須）
   - ユーザーはそのメッセージを取引先へそのまま転送する
5. チャットでもURLを単独の行で報告（全角カッコで囲まない）

## 既公開マップ一覧

| 日付 | 題名 | スラッグ | 備考 |
|---|---|---|---|
| 2026-07-28 | 東山物流　全体図 | hy-k3n8f | 東海市・草刈り現場、写真23枚 |

## 注意

- 公開URLは誰でも閲覧可（お客様の現場写真のため、公開前にユーザーの了承を得る。初回2026-07-28に方式は了承済み）
- 株数・個人情報のような機微情報はマップに含めない
- メーカーの改修時は `node -e` でscriptブロックの構文チェックをしてからプッシュ
